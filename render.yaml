services:
  # Background Worker for K-Score calculations
  - type: worker
    name: holdex-calculator
    runtime: node
    plan: starter
    region: oregon
    repo: https://github.com/zeyxx/HolDex
    branch: kscore-v8-rebased
    buildCommand: npm install --ignore-optional
    startCommand: npm run calculator
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: HELIUS_API_KEY
        sync: false
